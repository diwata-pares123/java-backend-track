spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require&currentSchema=delivery_schema
spring.datasource.username=postgres.vggyqelczapthdayxuhx
spring.datasource.password=MTRDM191814_MarcusTheodiosn
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.default_schema=delivery_schema
server.port=8081
server.error.include-message=always
# Tell Hibernate exactly what dialect to use so it stops trying to 'probe' the DB on startup
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Expose all actuator endpoints over HTTP
management.endpoints.web.exposure.include=*
# Enable the Prometheus endpoint specifically
management.endpoint.prometheus.enabled=true

# Show full details of all health components
management.endpoint.health.show-details=always
# Enable the specific Liveness and Readiness groups
management.endpoint.health.probes.enabled=true